name: build
on:
    push:
        branches:
        - sociosFabrica
        - dev
    pull_request:
        types: [opened, synchronize, reopened]
jobs:
    build:
        name: Build
        runs-on: ubuntu-latest
        strategy:
            matrix:
                java-version: ["11", "17"]

        steps:
            - uses: actions/checkout@v3
            -   name: Configurar Java
                uses: actions/setup-java@v3
                with:
                    java-version: 11

            -   name: Compile with Maven
                run: mvn -B package --file pom.xml

            -   name: Ejecutar pruebas
                run: mvn test
            - name: Cache SonarCloud packages
              uses: actions/cache@v3
              with:
                  path: ~/.sonar/cache
                  key: ${{ runner.os }}-sonar
                  restore-keys: ${{ runner.os }}-sonar
            - name: Cache Maven packages
              uses: actions/cache@v3
              with:
                  path: ~/.m2
                  key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
                  restore-keys: ${{ runner.os }}-m2
            - name: Build and analyze
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # Needed to get PR information, if any
                  SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
              run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=CodeF-ctory2023_back-virtual
    deploy:
        name: Deploy
        needs: build
        runs-on: ubuntu-latest

        steps:
        -   name: Desplegar en servidor
            run: |
                # Como no es claro donde y como se va a desplegar, este script aún esta pendiente por terminar. 
